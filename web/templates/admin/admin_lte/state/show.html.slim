.container-fluid
  .row
    .col-md-12
      .box
        .box-header.with-border
          .box-title States
        .box-body.table-responsive.no-padding
          .panel_contents
            table.table.table-hover
              tbody
                
                  - {name, value} = ExAdmin.View.admin_resource(@conn).get_show_field(@resource, :name)
                  tr
                    th= name
                    td= value
                
                  - {name, value} = ExAdmin.View.admin_resource(@conn).get_show_field(@resource, :code)
                  tr
                    th= name
                    td= value
                
                  - {name, value} = ExAdmin.View.admin_resource(@conn).get_show_field(@resource, :country_id)
                  tr
                    th= name
                    td= value
                