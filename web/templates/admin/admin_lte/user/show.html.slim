.container-fluid
  .row
    .col-md-12
      .box
        .box-header.with-border
          .box-title Users
        .box-body.table-responsive.no-padding
          .panel_contents
            table.table.table-hover
              tbody
                
                  - {name, value} = ExAdmin.View.admin_resource(@conn).get_schema_field(:show, @resource, :name)
                  tr
                    th= name
                    td= value
                
                  - {name, value} = ExAdmin.View.admin_resource(@conn).get_schema_field(:show, @resource, :email)
                  tr
                    th= name
                    td= value
                
                  - {name, value} = ExAdmin.View.admin_resource(@conn).get_schema_field(:show, @resource, :user_name)
                  tr
                    th= name
                    td= value
                
                  - {name, value} = ExAdmin.View.admin_resource(@conn).get_schema_field(:show, @resource, :street_num)
                  tr
                    th= name
                    td= value
                
                  - {name, value} = ExAdmin.View.admin_resource(@conn).get_schema_field(:show, @resource, :street)
                  tr
                    th= name
                    td= value
                
                  - {name, value} = ExAdmin.View.admin_resource(@conn).get_schema_field(:show, @resource, :city)
                  tr
                    th= name
                    td= value
                
                  - {name, value} = ExAdmin.View.admin_resource(@conn).get_schema_field(:show, @resource, :zip_code)
                  tr
                    th= name
                    td= value
                
                  - {name, value} = ExAdmin.View.admin_resource(@conn).get_schema_field(:show, @resource, :active)
                  tr
                    th= name
                    td= value
                
                  - {name, value} = ExAdmin.View.admin_resource(@conn).get_schema_field(:show, @resource, :birthdate)
                  tr
                    th= name
                    td= value
                
                  - {name, value} = ExAdmin.View.admin_resource(@conn).get_schema_field(:show, @resource, :height)
                  tr
                    th= name
                    td= value
                
                  - {name, value} = ExAdmin.View.admin_resource(@conn).get_schema_field(:show, @resource, :state_id)
                  tr
                    th= name
                    td= value
                
                  - {name, value} = ExAdmin.View.admin_resource(@conn).get_schema_field(:show, @resource, :country_id)
                  tr
                    th= name
                    td= value
                